name: signalr_pure
description: SignalR Pure is a library which contains some useful tools for Signalr. It's SignlR Client.
version: 1.0.0
homepage: https://dimolll.github.io/

environment:
  sdk: '>=2.14.2 <3.0.0'

dependencies:
  ansicolor: ^2.0.1
  meta: ^1.7.0
  tuple: ^2.0.0

dev_dependencies:
  build_runner: ^2.1.2
  mockito: ^5.0.16
  lints: ^1.0.0
  test: ^1.16.0
